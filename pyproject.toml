[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "secret-of-a-half"
version = "0.4.0"
description = "Information-spinor, native PhaseNav, Weil-Hermite and reciprocal self-duality research workspace."
requires-python = ">=3.10"
authors = [{name = "Adrian Lipa"}]
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
