\chapter{Zero, Undefinedness, and Reciprocal Self-Duality}
\label{ch:zero-undefined}

This chapter formalizes a new interpretive hypothesis: the half may be read as
the balanced state between a defined zero and an undefined informational
state.  The exact content is a labelled-state geometry.  The interpretation of
one label as ``undefined'' remains exploratory.

\section{The NaN boundary warning}

\begin{warning}[NaN is not a numeric endpoint]
IEEE floating-point \texttt{NaN} is not a real number and is not ordered with
zero.  Consequently, the statement that $1/2$ is numerically between
\texttt{NaN} and $0$ is not a well-formed assertion in the usual order on
$\mathbb R$.  In this chapter \texttt{NaN} appears only as a possible
implementation marker for an abstract state label.  It never enters arithmetic.
\end{warning}

Let
\[
 Z_0=\texttt{DEFINED\_ZERO},
 \qquad
 U=\texttt{UNDEFINED\_BOTTOM}
\]
be two labels.  They are vertices of a binary simplex, not numerical values to
be added together.

\section{The labelled binary simplex}

\begin{definition}[Zero--undefined state]
For $p\in[0,1]$, define
\[
 X(p)=(1-p)Z_0+pU.
\]
This notation denotes the probability distribution $(1-p,p)$ on the two
labels $Z_0$ and $U$.
\end{definition}

The exchange of labels induces the complement involution
\[
 C(p)=1-p.
\]
Its unique fixed point is immediate:
\[
 C(p)=p
 \quad\Longleftrightarrow\quad
 p=\frac12.
\]

\section{Projective odds and reciprocal inversion}

To avoid collision with the centred zeta coordinate used elsewhere, write
$z_{\rm o}$ for the projective odds coordinate:
\[
 z_{\rm o}=\Omega(p)=\frac{p}{1-p}.
\]
It compactifies the two pure labelled states as
\[
 p=0\longleftrightarrow z_{\rm o}=0,
 \qquad
 p=1\longleftrightarrow z_{\rm o}=\infty.
\]

\begin{theorem}[Reciprocal conjugacy]
\label{thm:zero-undefined-conjugacy}
For $p\in(0,1)$,
\[
 \Omega(C(p))=\frac1{\Omega(p)}.
\]
With the endpoint conventions $1/0=\infty$ and $1/\infty=0$, the identity
extends to the closed simplex.
\end{theorem}

\begin{proof}
Directly,
\[
 \Omega(1-p)=\frac{1-p}{p}
 =\frac1{p/(1-p)}
 =\frac1{\Omega(p)}.
\]
The endpoint statement follows by continuity on the extended non-negative
line.
\end{proof}

\begin{corollary}[Unique positive self-dual point]
\label{cor:zero-undefined-half}
The reciprocal map $z_{\rm o}\mapsto1/z_{\rm o}$ has the unique positive fixed
point $z_{\rm o}=1$, and
\[
 \Omega^{-1}(1)=\frac12.
\]
Hence the projective self-dual point and the label-swap fixed point coincide.
\end{corollary}

\section{Spinor embedding}

Define the positive amplitude representative
\[
 \Psi(p)=
 \begin{pmatrix}
 \sqrt{1-p}\\
 \sqrt p
 \end{pmatrix}.
\]
The exchange matrix
\[
 X=\begin{pmatrix}0&1\\1&0\end{pmatrix}
\]
satisfies $X\Psi(p)=\Psi(1-p)$.  Its unique positive normalized fixed state is
\[
 \Psi\!\left(\frac12\right)
 =\frac1{\sqrt2}
 \begin{pmatrix}1\\1\end{pmatrix}.
\]
Thus the same half selected by reciprocal projective duality is the equal-gain
spinor fixed by channel exchange.

\section{Fisher--Rao midpoint}

The Bernoulli Fisher metric is
\[
 ds^2=\frac{dp^2}{p(1-p)}.
\]
Introduce the arc coordinate
\[
 \theta(p)=2\arcsin\sqrt p.
\]
Then $ds^2=d\theta^2$, while
\[
 \theta(0)=0,
 \qquad
 \theta(1)=\pi.
\]

\begin{theorem}[Geodesic midpoint]
\label{thm:fisher-half-midpoint}
The state $p=1/2$ is the exact Fisher--Rao midpoint of the two pure labelled
states:
\[
 d_{\rm FR}(0,1/2)=d_{\rm FR}(1/2,1)=\frac\pi2.
\]
\end{theorem}

\begin{proof}
Since $\theta(1/2)=2\arcsin(1/\sqrt2)=\pi/2$, the distances in the arc
coordinate are both $\pi/2$.
\end{proof}

The same point maximizes binary entropy:
\[
 H_2(p)=-p\ln p-(1-p)\ln(1-p),
 \qquad
 H_2(1/2)=\ln2.
\]
Consequently four exact selections coincide:
\[
\begin{aligned}
 \text{label-swap fixed point}
 &=\text{reciprocal fixed point}\\
 &=\text{spinor balance}\\
 &=\text{Fisher--Rao midpoint}\\
 &=\frac12.
\end{aligned}
\]

\section{Self-duality does not by itself imply a dynamical extremum}
\label{sec:half-self-dual-not-maximum}

The preceding fixed-point statements are exact, but they must not be enlarged
into the stronger assertion that every reciprocal-symmetric dynamical statistic
is maximized at the self-dual point.  DHSE-001 Stage M gives an exact finite
counterexample to that inference.

In projective odds $q>0$, let a primitive positive integer M\"obius universe
with coefficient bound $K=6$ act by binary words of lengths $n=1,2,3,4$.  For
the projective target radius $r=1/10$, each admissible word contributes an exact
closed rational interval of target centres.  The resulting forcing-count
function $N_n(q)$ is therefore an integer-valued step function whose complete
maximum can be classified by a finite rational endpoint sweep.

The universe is closed under reciprocal conjugation, so
\[
 N_n(q)=N_n(1/q).
\]
Nevertheless, the exact maximizers are length dependent:
\[
\begin{array}{c|c}
 n & \operatorname*{arg\,max}_{q>0}N_n(q)\\
\hline
1 & [9/11,11/12]\cup[12/11,11/9]\\
2 & \{1\}\\
3 & \{1\}\\
4 & [9882/9911,341/342]\cup[342/341,9911/9882].
\end{array}
\]
Thus $q=1$, equivalently $p=1/2$, is the unique global maximizer for lengths 2
and 3 but is not a global maximizer for lengths 1 and 4.  At length 4 the split
is a narrow reciprocal doublet immediately adjacent to the self-dual point.

\status{SOH-L022: exact finite computer-assisted theorem on the declared
$K=6$, $r=1/10$, $n\le4$ universe.}

The formal lesson is precise:
\[
\boxed{
\text{reciprocal symmetry}
\;\not\Rightarrow\;
\text{central maximum}
}
\]
without an additional convexity, monotonicity, positivity, or comparable
structural condition.  This negative result strengthens the claim boundary of
the monograph: the special status of the half as a fixed point, entropy maximum,
and Fisher--Rao midpoint is exact, while any further extremal statement for a
new dynamical functional requires its own theorem.

The reproducible certificate and implementation are maintained in
\texttt{docs/experiments/DHSE-001-STAGE-M-CONTINUOUS-THEOREM.md} and
\texttt{src/secret\_of\_a\_half/dhse\_001\_stage\_m.py}.

\section{Native PhaseNav formulation}

The authoritative executable source is
\begin{center}
\texttt{construction/phasenav/secret\_of\_half\_zero\_undefined\_duality.pnv}.
\end{center}
It declares the vertices, complement orbit, projective odds transform,
reciprocal orbit, spinor embedding, Fisher--Rao coordinate and the explicit
NaN guard.  Python parses and audits that source; it is not the source of the
construction.

\section{Claim ledger}

\begin{itemize}
\item \textbf{SOH-L015 (exact):} complement is conjugate to reciprocal inversion
in odds coordinates.
\item \textbf{SOH-L016 (exact):} the unique positive reciprocal fixed point
corresponds to $p=1/2$.
\item \textbf{SOH-L017 (exact):} $p=1/2$ is the Fisher--Rao geodesic midpoint.
\item \textbf{SOH-L022 (exact finite computer-assisted):} in the declared
DHSE-001 Stage M universe, the complete forcing landscape is classified and the
self-dual maximum is length dependent.
\item \textbf{SOH-D001 (exact finite counterexample):} reciprocal symmetry alone
does not force a global maximum at the self-dual point.
\item \textbf{SOH-H001 (exploratory):} defined zero and undefined bottom are
interpreted as informational boundary labels.
\end{itemize}

\section{Boundary relative to the zeta problem}

The exact lemmas in this chapter, including the Stage M finite theorem, do not
identify non-trivial zeta zeros with zero--undefined states.  They therefore do
not close \texttt{SOH-C004} or \texttt{SOH-C005}.  Their role is structural:
they characterize what reciprocal self-duality does and does not imply.  Any
use in a critical-line theorem still requires a canonical bridge from the
completed zeta function to the labelled PhaseNav state.
