# Zero–Undefined Reciprocal Duality claim ledger

| ID | Statement | Status | Evidence | Blocking gap |
|---|---|---|---|---|
| SOH-L015 | In odds coordinates, binary complement is conjugate to reciprocal inversion: `Omega(1-p)=1/Omega(p)`. | Exact lemma | Direct algebra; native `.pnv`; regression tests | None |
| SOH-L016 | The unique positive reciprocal fixed point `z=1` corresponds to the unique complement-fixed weight `p=1/2`. | Exact lemma | Solve `z=1/z` and invert odds | None |
| SOH-L017 | `p=1/2` is the Fisher–Rao geodesic midpoint between the two pure labelled vertices. | Exact information-geometric lemma | Arc coordinate `theta=2 asin sqrt(p)` | None |
| SOH-H001 | `DEFINED_ZERO` and `UNDEFINED_BOTTOM` are interpreted as informational boundary labels whose balanced state is one half. | Exploratory hypothesis | Structural synthesis | Canonical physical or zeta-state interpretation |

## Non-claim

IEEE NaN is not a numeric vertex and is not ordered with zero.  It is only an
implementation marker for `UNDEFINED_BOTTOM`.  These claims do not prove RH.
