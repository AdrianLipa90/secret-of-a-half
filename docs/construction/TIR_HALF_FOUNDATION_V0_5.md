# TIR × ½ Foundation Crosswalk v0.5

This integration imports the recovered TIR information/action dependency axis
into Secret-of-a-Half without weakening the RH proof firewall.

## Shared exact/project-derived axis

`1/2 -> normalized recurrence -> information per cycle -> kappa`

with:

`Delta I_cycle = ln(2)/12`

and, for `C=2*pi`,

`kappa = ln(2)/(24*pi)`.

## Charged-lepton dependency export

The existing TIR pre-benchmark gates share the discrete basis:

`G(h,a,b,c) = h/2 + a*kappa + b*kappa/L3 + c*kappa^2/2`.

The bridge exports coefficient states and their PhaseNav 36D envelopes as
dependencies only. It does not assert that charged-lepton phenomenology proves
the zeta half-axis construction, nor vice versa.

## Strict boundary

The following remain open:

- SOH-C004: every non-trivial Xi zero closes in the canonical self-dual shell;
- SOH-C005: dense-family arithmetic Weil positivity and null-space -> closure;
- derivation of the physical sign/orientation/role assignment for the discrete
  TIR coefficient tuples.

Any solver consuming this bridge must preserve those statuses.
