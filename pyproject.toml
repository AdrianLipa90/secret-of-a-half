[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "secret-of-a-half"
version = "0.5.0"
description = "Information-spinor, native PhaseNav, Weil-Hermite, reciprocal self-duality, and prime-tail certification workspace."
requires-python = ">=3.10"
authors = [{name = "Adrian Lipa"}]
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
