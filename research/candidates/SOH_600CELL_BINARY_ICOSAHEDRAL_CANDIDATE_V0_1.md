# SOH 600-cell / Binary Icosahedral Candidate v0.1

Status: `CANDIDATE / ACTIVE_WORKING_DEPENDENCY / RIEMANN_CLAIMS_UNCHANGED`

Date: 2026-09-11

SOH adopts the shared rule that `CANDIDATE` is an active working state. Candidate material may be executed, tested and used by later candidate surfaces while preserving explicit candidate provenance.

The current candidate bridge is:

```text
600-cell vertices on S^3
        |
        v
finite SU(2)-compatible / binary-icosahedral research surface
```

The validated upstream carrier is the TIR/PNCS 600-cell candidate with exact finite tests for the 120-point S^3 carrier and the low angular sectors ell=0..5.

This file registers the bridge for SOH experiments involving spinorial, half-orbit, phase and representation-theoretic structure. It does NOT assert that the bridge proves the Riemann hypothesis, fixes any existing SOH no-go result, or converts a candidate ansatz into a theorem.

Candidate results may be consumed by later SOH candidate tests. Promotion requires an explicit theorem/gate showing which SOH claim is strengthened and why the result is independent of numerical fitting.
