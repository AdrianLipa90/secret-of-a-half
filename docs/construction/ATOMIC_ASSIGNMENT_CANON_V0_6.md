# Atomic Assignment Canon v0.6

This layer connects the TIR discrete action generator to pre-coefficient PhaseNav/orbital geometry without weakening the Secret-of-a-Half proof firewall.

## Structural role routing

For `G(h,a,b,c)=h/2+a*kappa+b*kappa/L3+c*kappa^2/2`:

- `h` is owned by the projective CP1 / spin-half identity layer;
- `a` is owned by the generation seed / Ramanujan release layer;
- `b` is owned by the Collatz terminal-return/orbit layer;
- `c` is owned by tetrahedral -> Poincare -> Berry curvature/holonomy.

Status: `STRUCTURAL_ROLE_ROUTING_PASS`.

## Orientation source

The CIEL orbital runtime supplies the mass-free rule

`spin=tanh(-alpha_s*dV_dphi)`.

For every positive `alpha_s`, its sign is exactly `sign(-dV_dphi)`. The numerical magnitude of `alpha_s` therefore does not choose the orientation class.

The independent orbital relation contract is directed: `attractor -> satellite`; outgoing phase contributes positively while incoming/satellite phase contributes negatively to the aggregate phase.

These are source operators. Their mapping onto individual TIR coefficient signs is still a candidate binding and requires a prospective pre-coefficient test.

## No-circularity gate

An atomic assignment test is invalid if its inputs contain observed masses, fitted Yukawas, target actions/releases, an already known coefficient tuple, or a 36D envelope generated from that tuple.

PhaseNav 36D is the routing/holonomy representation of an independently constructed state; it cannot serve as self-evidence for that same state.

## Collatz observation

Under the earlier no-mass-input generation assignment the seed centers have stopping lengths `2,9,8`. The two recovered lepton transitions retrospectively match `sign(ell_dst-ell_src)` and `|c|=ell_dst-1`.

This pattern is explicitly **not promoted**. Two known transitions are insufficient and the relation was recognized after the coefficient states were recovered.

## Secret-of-a-Half boundary

`SOH-C004` and `SOH-C005` remain OPEN. Nothing in this atomic assignment layer proves the Riemann hypothesis, dense-family Weil positivity, or zero-to-native-closure.