# TIR × ½ Foundation Crosswalk v0.7

This revision binds the previously recovered TIR × ½ dependency axis to the
canonical PhaseNav dependency firewall and the NOEMA provenance contract.

## Executable parents

- PhaseNav: `54f65f2ca7d35cdd98f0ab8984cc1a8d74444a96`
- NOEMA: `42a0a8916e81ca27f2213bf0f28538f046c2e89a`

PhaseNav owns executable T^36 routing. NOEMA owns dependency/provenance state.
Neither repository gains authority to promote a mathematical claim in this
repository.

## Shared information-phase axis

`1/2 -> normalized recurrence -> information per cycle -> kappa`

with

`Delta I_cycle = ln(2)/12`

and, for `C=2*pi`,

`kappa = ln(2)/(24*pi)`.

The status of `kappa` remains `PROJECT_DERIVED_EXACT_ALGEBRA`; executable use is
not a claim that it is an externally established physical constant.

## Discrete coefficient state and PhaseNav envelope

The TIR coefficient generator remains

`G(h,a,b,c) = h/2 + a*kappa + b*kappa/L3 + c*kappa^2/2`.

Its intrinsic state is 4D. The PhaseNav transport envelope is 36D. The bridge
forbids treating the 36D vector as a source of coefficient values.

## Assignment status refined from v0.5

v0.5 treated the whole role/orientation assignment problem as open. v0.7 keeps
the remaining gap but separates already implemented structure:

- `ROLE_ROUTER_HABC = STRUCTURAL_ROLE_ROUTING_PASS`;
- `ORBIT_DIRECTION = IMPLEMENTED_PROJECT_ORBITAL_RULE`;
- `RELATIONAL_GRADIENT = IMPLEMENTED_SOURCE_OPERATOR_CANDIDATE_TIR_BINDING`;
- `TIR_SLOT_BINDING = OPEN`.

The open promotion target is now precise: produce a prospective map from
coefficient-free atomic/orbital state to sign, orientation, and the recovered
coefficient tuple without using measured masses, measured Yukawas, or
coefficient-enriched vectors as parents.

## Riemann-Hypothesis firewall unchanged

The software integration does not close the mathematical bridge:

- `SOH-C004` remains OPEN: every non-trivial Xi zero must be shown to close in
  the canonical self-dual/native PhaseNav shell;
- `SOH-C005` remains OPEN: dense-family arithmetic Weil positivity,
  regularization, and the null-space-to-native-closure implication remain to be
  established.

No PhaseNav route, NOEMA memory receipt, TIR phenomenological match, or finite
numerical positivity sample is allowed to promote those claims.

## Cross-repository scientific debt

The bridge also carries the current TIR methodological/physical statuses as
provenance only. In particular the common-likelihood construction remains OPEN
and the current neutron-EDM relation remains PHYSICAL_FAIL.

This keeps the two research programmes connected where their formal dependency
is explicit while preserving their independent proof and falsification ledgers.
