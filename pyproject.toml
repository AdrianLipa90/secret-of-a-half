[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "secret-of-a-half"
version = "0.1.0"
description = "Independent computational and mathematical workspace for the information-spinor critical-axis ansatz."
requires-python = ">=3.10"
authors = [{name = "Adrian Lipa"}]
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
